

const calculateReducer = (state: any, action: any) => {
   switch (action.type) {
      case 'SET_ITEMS':
         return state
      default:
         return state
   }
}

export default calculateReducer